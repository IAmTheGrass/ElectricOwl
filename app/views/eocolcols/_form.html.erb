<%= form_for @eocolcol,remote: true do |f| %>
<%= f.label :eoobject_id %>
<%= f.collection_select :eoobject_id, Eoobject.all, :id, :name %>
<%= f.label :eocolumn_id %>
<%= f.collection_select :eocolumn_id, Eocolumn.all, :id, :name %>
<%= f.submit  %>
<% end %>