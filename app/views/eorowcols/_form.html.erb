<%= form_for @eorowcol,remote: true do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :eoobject_id %>
<%= f.collection_select :eoobject_id, Eoobject.all, :id, :name %>
<%= f.label :eorow_id %>
<%= f.collection_select :eorow_id, Eorow.all, :id, :name %>
<%= f.submit  %>
<% end %>